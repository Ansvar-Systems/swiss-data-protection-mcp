# Gitleaks configuration for swiss-data-protection-mcp
# https://github.com/gitleaks/gitleaks

title = "swiss-data-protection-mcp - Secret Detection"

[extend]
# Use default gitleaks rules
useDefault = true

[allowlist]
description = "Known false positives and example credentials"

# Example/development tokens in documentation
regexes = [
  '''dev-token''',
  '''example-token''',
  '''your-token-here''',
  '''<password>''',
  '''user:pass''',
]

# Files to skip (narrowly scoped to known false-positive patterns)
paths = [
  '''tests/fixtures/.*''',
  '''data/seed/.*\.json$''',
  '''\.env\.local$''',
  '''\.env\..*\.local$''',
]
